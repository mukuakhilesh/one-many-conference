Creating one to many connection using webRTC

install the required package using:
    npm install

run server using :
    npm start

admin:
    localhost:3000/index-admin.html

client:
    localhost:3000/index.html
    